import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Shopping Cart | Modern Storefront',
  description: 'Review your shopping cart and proceed to checkout. Secure payment processing with fast shipping.',
  robots: 'noindex, nofollow', // Cart pages typically shouldn't be indexed
};

