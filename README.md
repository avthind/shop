# Shop Storefront

A clean, minimal, mobile-first Next.js storefront for selling products.

## Getting Started

First, run the development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Project Structure

- `/app` - Next.js app router pages and layouts
- `/components` - Reusable React components
- `/context` - React context for cart and wishlist state
- `/data` - Placeholder product data
- `/types` - TypeScript type definitions
- `/styles` - Global CSS and design tokens

## Features

- Product gallery with filtering and sorting
- Shopping cart functionality
- Wishlist/favorites
- Checkout flow (placeholder for Stripe/PayPal)
- User account page
- Mobile-first responsive design
- Accessible UI components

## Next Steps

1. Replace placeholder product data with real Etsy/Printify data
2. Integrate payment processing (Stripe/PayPal)
3. Add authentication for user accounts
4. Connect to backend API for order management
5. Add SEO meta tags and optimization

